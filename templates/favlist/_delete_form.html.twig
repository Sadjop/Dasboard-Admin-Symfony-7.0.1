<form method="post" action="{{ path('app_favlist_delete', {'id': favlist.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favlist.id) }}">
    <button class="btn">Delete</button>
</form>
